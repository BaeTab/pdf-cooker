# Google Analytics 설정 가이드

PDF Cooker에 Google Analytics 4 (GA4)가 설정되어 있습니다. 실제 사용을 위해서는 다음 단계를 따라주세요.

## 📋 설정 단계

### 1. Google Analytics 계정 생성

1. [Google Analytics](https://analytics.google.com/)에 접속
2. "측정 시작" 클릭
3. 계정 이름 입력 (예: "PDF Cooker")
4. 속성 이름 입력 (예: "PDF Cooker Website")
5. **보고 시간대**: 대한민국
6. **통화**: 대한민국 원 (₩)

### 2. 데이터 스트림 생성

1. 플랫폼 선택: **웹**
2. 웹사이트 URL 입력 (예: `https://your-domain.com`)
3. 스트림 이름 입력 (예: "PDF Cooker Web")
4. "스트림 만들기" 클릭

### 3. 측정 ID 복사

데이터 스트림을 만들면 **측정 ID**가 생성됩니다.
- 형식: `G-XXXXXXXXXX`
- 예시: `G-ABC123DEF4`

이 ID를 복사해두세요.

### 4. 코드에 측정 ID 적용

`index.html` 파일을 열고 **두 곳**의 `G-XXXXXXXXXX`를 실제 측정 ID로 교체하세요:

```html
<!-- index.html -->

<!-- Google Analytics 4 (GA4) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-YOUR-ACTUAL-ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-YOUR-ACTUAL-ID', {  <!-- 여기도 변경! -->
    'send_page_view': true,
    'cookie_flags': 'SameSite=None;Secure'
  });
</script>
```

### 5. 배포 및 확인

1. 변경사항을 저장하고 배포
2. Google Analytics 대시보드에서 "실시간" 보고서 확인
3. 웹사이트를 방문하면 실시간 사용자가 표시됩니다

## 📊 추적되는 이벤트

PDF Cooker는 다음 이벤트를 자동으로 추적합니다:

### 페이지 뷰
- 자동으로 페이지 방문 추적

### 탭 변경
- **이벤트명**: `tab_change`
- **매개변수**: `tab_name` (merge, split, convert)

### 파일 업로드
- **이벤트명**: `file_upload`
- **매개변수**: 
  - `file_count`: 업로드된 파일 개수
  - `total_size_mb`: 총 파일 크기 (MB)

### PDF 병합
- **시작**: `pdf_merge_start`
  - `value`: 파일 개수
- **성공**: `pdf_merge_success`
  - `file_count`: 병합된 파일 개수
  - `total_size_mb`: 총 파일 크기
- **오류**: `pdf_merge_error`
  - `error_message`: 오류 메시지

### PDF 분할
- **시작**: `pdf_split_start`
  - `split_mode`: 분할 방식 (all/range)
  - `page_count`: 페이지 수
- **성공**: `pdf_split_success`
  - `split_mode`: 분할 방식
  - `output_count`: 출력 파일 개수
- **오류**: `pdf_split_error`
  - `error_message`: 오류 메시지

### PDF → 이미지 변환
- **시작**: `pdf_convert_start`
  - `format`: 이미지 포맷 (jpeg/png)
  - `quality`: 품질 (1-3)
  - `page_count`: 페이지 수
- **성공**: `pdf_convert_success`
  - `format`, `quality`, `page_count`
- **오류**: `pdf_convert_error`
  - `error_message`: 오류 메시지

## 🔍 Google Analytics에서 이벤트 확인하기

### 실시간 이벤트 확인
1. Google Analytics 대시보드 접속
2. 좌측 메뉴에서 **보고서** > **실시간** 클릭
3. 사용자가 작업을 수행하면 실시간으로 이벤트 표시

### 이벤트 보고서
1. **보고서** > **참여도** > **이벤트** 클릭
2. 각 이벤트의 발생 횟수 확인
3. 이벤트 클릭 시 매개변수 상세 정보 확인

### 맞춤 보고서 만들기
1. **탐색** > **빈 보고서** 클릭
2. 측정기준: 이벤트 이름, 이벤트 매개변수
3. 측정항목: 이벤트 수, 사용자 수
4. 원하는 형태로 보고서 구성

## 🎯 유용한 분석 지표

### 사용자 행동 분석
- 어떤 기능을 가장 많이 사용하는가? (merge/split/convert)
- 평균 파일 크기는?
- 오류 발생률은?

### 전환율 분석
- 페이지 방문 → 파일 업로드 전환율
- 파일 업로드 → 작업 완료 전환율

### 성능 분석
- 어떤 작업에서 오류가 많이 발생하는가?
- 사용자들이 어떤 이미지 포맷을 선호하는가?

## 🔧 추가 커스터마이징

더 많은 이벤트를 추적하고 싶다면 `src/utils/analytics.js` 파일을 수정하세요:

```javascript
// 예시: 새로운 이벤트 추가
export const PDFEvents = {
  // ... 기존 이벤트들 ...
  
  // 새로운 이벤트
  customEvent: (param1, param2) => {
    trackEvent('custom_event_name', {
      event_category: 'Category',
      event_label: 'Label',
      param1: param1,
      param2: param2,
    });
  },
};
```

## ⚠️ 주의사항

1. **개인정보 보호**: 파일 이름이나 내용은 절대 추적하지 않습니다
2. **GDPR 준수**: EU 사용자를 위한 쿠키 동의 배너 추가 권장
3. **데이터 보존**: Google Analytics 설정에서 데이터 보존 기간 설정

## 📚 참고 자료

- [Google Analytics 4 공식 문서](https://support.google.com/analytics/answer/10089681)
- [GA4 이벤트 측정](https://developers.google.com/analytics/devguides/collection/ga4/events)
- [gtag.js 참조](https://developers.google.com/analytics/devguides/collection/gtagjs)

---

설정 완료 후 문제가 있다면 브라우저 콘솔에서 `dataLayer` 객체를 확인하여 이벤트가 제대로 전송되는지 확인할 수 있습니다:

```javascript
console.log(window.dataLayer);
```
